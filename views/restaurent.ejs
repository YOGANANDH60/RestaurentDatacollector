<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Eatwell- make the world a tastier place</title>
    <link rel="stylesheet" href="styles/shared.css"/>
    <link rel="stylesheet" href="styles/recommend.css"/>
    <link rel="stylesheet" href="styles/restaurentdata.css"/>
   
</head>
<body>
    <header id="main-header">
        <div id="logo"><a href="/">Eatwell</a></div>
        <nav>
            <ul>
                <li><a href="/restaurent">browse resturent</a></li>
                <li><a href="/recommend">share a resturent</a></li>
                <li><a href="about">about Eatwell</a></li>
            </ul>
        </nav>
    </header>
    <main>Recommended Restaurent
    <% if (numberofrestaurent === 0) { %>
        <p>unfortunately there is no restaurent is register</p>
    <% } else { %>
        <p>find your next fav restaurent registered</p>
        <p>we found <%= numberofrestaurent %>restaurent</p>
        <ol id="restaurents-list">
        <% for(const restaurent of restaurents) { %>
            <li class="restaurent-item">
                <h2><%= restaurent.name %></h2> 
                <div class="restaurent-data">
                    <p><%= restaurent.cuision %></p>
                    <p><%= restaurent.address %></p>
                </div>
                <p><%= restaurent.discription %></p>
                <div id="visitwrab">
                  <a id="visit" href="/restaurents/<%= restaurent.id %>">view website</a>
                </div>
            </li>
        <% } %>
    </ol>
    <% } %>
</main>
</body>
</html>